using System;
using System.Data.SqlClient;
using proyectoVdufferx.Properties;

namespace proyectoVdufferx
{/*
    public class fotografiaDAO
    {
        public static bool FotografiaNueva(fotofgrafia f)
        {
            bool exito = true;
            try
            {
                string cadena = Resources.cadena_conexion;
                using (SqlConnection connection = new SqlConnection(cadena))
                {
                    string query =
                        "INSER INTO USUARIO(fotografia)VALUES" +
                }
            }
            catch (Exception)
            {
                exito = false;
            }

            return exito;
        }
    }*/
}